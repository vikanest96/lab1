## Отчет по лабораторной работе № 1

#### № группы: `ПМ-2502`

#### Выполнила: `Нестерчук Виктория Юрьевна`

#### Вариант: `12`

### Cодержание:

- [Постановка задачи](#1-постановка-задачи)
- [Входные и выходные данные](#2-входные-и-выходные-данные)
- [Выбор структуры данных](#3-выбор-структуры-данных)
- [Алгоритм](#4-алгоритм)
- [Программа](#5-программа)
- [Анализ правильности решения](#6-анализ-правильности-решения)

### 1. Постановка задачи

> Программа получает на вход 5 чисел X,A,B,C,D. Последовательность из четырех ванночек объемами A, B, C, D литров установлена в указанном порядке ступенькой (объем A — сверху). Ванночки установлены таким образом, что если в ванночку наливать воду больше её
объема, излишки будут стекать в следующую ванночку, установленную ниже (как каскадный водопад). Изначально все ванночки пусты. В каждую
ванночку выливают воду объемом X литров. Какое количество ванночек
окажется в результате полностью заполненным?

### 2. Входные и выходные данные

#### Данные на вход

На вход программа должна получать 5 натуральных числа.

|             | Тип                | 
|-------------|--------------------|
| X (Число 1) | Натуральное число  | 
| A (Число 2) | Натуральное число  |
| B (Число 3) | Натуральное число  |
| C (Число 4) | Натуральное число  |
| D (Число 5) | Натуральное число  |

#### Данные на выход

Т.к. программа должна вывести количество заполненных ванночек, то на выход мы получим
единственное целое неотрицательное число, от 0 до 4.

|         | Тип                                | min значение | max значение   |
|---------|------------------------------------|--------------|----------------|
| Число 1 | целое неотрицательное число        | 0            | 4              |

### 3. Выбор структуры данных

Программа получает 5 натуральных числа. Поэтому для их хранения
можно выделить 5 переменных (`X`,`A`,`B`,`C`,`D`) типа `int`.

|             | название переменной | Тип (в Java) | 
|-------------|---------------------|--------------|
| X (Число 1) | `X`                 | `int`        |
| A (Число 2) | `A`                 | `int`        |
| B (Число 3) | `B`                 | `int`        |
| C (Число 4) | `C`                 | `int`        |
| D (Число 5) | `D`                 | `int`        |

Для вывода результата необязательно его хранить в отдельной переменной.

### 4. Алгоритм

#### Алгоритм выполнения программы:

1. **Ввод данных:**  
   Программа считывает пять натуральных чисел, обозначенные как `X`,`A`,`B`,`C`,`D`.

2. **Сравнение чисел:**
   1)Программа сравнивает числа A и X.
   
   1.1)Если X >= A выполняется, то ванночка объема A литров считается заполненной. Далее программа сравнивает числа B и X.
   
   2.1)Если 2*X - A >= B выполняется, то ванночка объема B литров считается заполненной. Далее программа сравнивает числа C и X.
   
   3.1)Если 3*X - A - B >= C выполняется, то ванночка объема C литров считается заполненной. Далее программа сравнивает числа D и X.
   
   4.1)Если 4*X - A - B - C >= D выполняется, то ванночка объема D литров считается заполненной.
   
   В таком случае программа выводит 4 , так как все ванночки оказались заполненными.
   
   4.2)Если 4*X - A - B - C >= D не выполняется, то ванночка объема D литров считается не заполненной.
   
   В таком случае программа выводит 3 , так как только ванночка объемом D оказалась не заполненной.
   
   3.2)Если 3*X - A - B >= C не выполняется, то ванночка объема C литров считается не заполненной. Далее программа сравнивает числа D и X.
   
   4.3)Если X >= D выполняется, то ванночка объема D литров считается заполненной.
   
   В таком случае программа выводит 3 , так как только ванночка объемом С оказалась не заполненной.
   
   4.4)Если X >= D не выполняется, то ванночка объема не D литров считается заполненной.
   
   В таком случае программа выводит 2 , так как только ванночки объемом С и D оказались не заполненными.
   
   2.2)Если 2*X - A >= B не выполняется, то ванночка объема B литров считается не заполненной. Далее программа сравнивает числа C и X.
   
   3.3)Если X >= C выполняется, то ванночка объема C литров считается заполненной. Далее программа сравнивает числа D и X.
   
   4.5)Если 2*X - С >= D выполняется, то ванночка объема D литров считается заполненной.
   
   В таком случае программа выводит 3 , так как только ванночка объемом B оказалась не заполненной.
   
   4.6)Если 2*X - С >= D выполняется, то ванночка объема D литров считается заполненной.
   
   В таком случае программа выводит 2 , так как только ванночки объемом B и D оказались не заполненными.
   
   3.4)Если X >= C не выполняется, то ванночка объема C литров считается не заполненной. Далее программа сравнивает числа D и X.

   4.7)Если X >= D выполняется, то ванночка объема D литров считается заполненной.

   В таком случае программа выводит 2 , так как только ванночки объемом B и C оказались не заполненными.

   4.8)Если X >= D не выполняется, то ванночка объема D литров считается не заполненной.

   В таком случае программа выводит 1 , так как только ванночка объемом A оказалась заполненной.

   1.2)Если X >= A не выполняется, то ванночка объема A литров считается не заполненной. Далее программа сравнивает числа B и X.
   
   2.3)Если X >= B выполняется, то ванночка объема B литров считается заполненной. Далее программа сравнивает числа C и X.
   
   3.5)Если 2*X - B >= C выполняется, то ванночка объема C литров считается заполненной. Далее программа сравнивает числа D и X.
   
   4.9)Если 3*X - B - С >= D выполняется, то ванночка объема D литров считается заполненной.

   В таком случае программа выводит 3 , так как только ванночка объемом A оказалась не заполненной.

   4.10)Если 3*X - B - С >= D не выполняется, то ванночка объема D литров считается не заполненной.

   В таком случае программа выводит 2 , так как только ванночки объемом A и D оказались не заполненными.

   3.6)Если 2*X - B >= C не выполняется, то ванночка объема C литров считается не заполненной. Далее программа сравнивает числа D и X.

   4.11)Если X >= D выполняется, то ванночка объема D литров считается заполненной.
   
   В таком случае программа выводит 2 , так как только ванночки объемом A и С оказались не заполненными.

   4.12)Если X >= D не выполняется, то ванночка объема D литров считается не заполненной.

   В таком случае программа выводит 1 , так как только ванночка объемом B оказалась заполненной.

   2.4)Если X >= B не выполняется, то ванночка объема B литров считается не заполненной. Далее программа сравнивает числа C и X.

   3.7)Если X >= C выполняется, то ванночка объема C литров считается заполненной. Далее программа сравнивает числа D и X.

   4.13)Если 2*X - С >= D выполняется, то ванночка объема D литров считается заполненной.

   В таком случае программа выводит 2 , так как только ванночки объемом A и B оказались не заполненными.

   4.14)Если 2*X - С >= D не выполняется, то ванночка объема D литров считается не заполненной.

   В таком случае программа выводит 1 , так как только ванночка объемом C оказалась заполненной.

   3.8)Если X >= C не выполняется, то ванночка объема C литров считается не заполненной. Далее программа сравнивает числа D и X.

   4.15)Если X >= D выполняется, то ванночка объема D литров считается заполненной.

   В таком случае программа выводит 1 , так как только ванночка объемом D оказалась заполненной.

   4.16)Если X >= D не выполняется, то ванночка объема D литров считается не заполненной.

   В таком случае программа выводит 0 , так как все ванночки оказались не заполненными.

   
4. **Вывод данных:**  
   Программа выводит одно целое неотрицательное число, количество заполненных ванночек.

#### Блок-схема

```mermaid
graph TD
    A([Начало]) --> B[/Ввод: X, A, B, C, D/]
    B --> C{X >= A}
    C -- Нет --> D{X >= B}
    D -- Нет --> E{X >= C}
    E -- Нет --> F{X >= D}
    F -- Нет --> H[/Вывод: 0/]
    F -- Да --> I[/Вывод: 1/]
    E -- Да --> J{2*X - C >= D}
    J -- Нет --> I[/Вывод: 1/]
    J -- Да --> K[/Вывод: 2/]
    D -- Да --> L{2*X - B >= C}
    L -- Да --> N{3*X - B - C >= D}
    N -- Нет --> K[/Вывод: 2/]
    N -- Да --> S[/Вывод: 3/]
    L -- Нет --> T{X >= D}
    T -- Нет --> I[/Вывод: 1/]
    T -- Да --> K[/Вывод: 2/]
    C -- Да --> M{2*X - A >= B}
    M -- Нет --> G{X >= C}
    M -- Да --> R{3*X - A - B >= C}
    G -- Да --> R1{2*X - C >= D}
    R1 -- Да --> S[/Вывод: 3/]
    R1 -- Нет --> K[/Вывод: 2/]
    G -- Нет --> R2{X >= D}
    R2 -- Нет --> I[/Вывод: 1/]
    R2 -- Да --> K[/Вывод: 2/]
    R -- Да --> R4{4*X - A - B - C >= D}
    R4 -- Да --> I1[/Вывод: 4/]
    R4 -- Нет --> S[/Вывод: 3/]
    R -- Нет --> R5{X >= D}
    R5 -- Нет --> K[/Вывод: 2/]
    R5 -- Да  --> S[/Вывод: 3/]


```
### 5. Программа

```java
import java.io.PrintStream;
import java.util.Scanner;

public class Main {
    // Объявляем объект класса Scanner для ввода данных
    public static Scanner in = new Scanner(System.in);
    // Объявляем объект класса PrintStream для вывода данных
    public static PrintStream out = System.out;
    // Вводим пять натуральных чисел с клавиатуры
    public static void main(String[] args) {
        int X = in.nextInt();
        int A = in.nextInt();
        int B = in.nextInt();
        int C = in.nextInt();
        int D = in.nextInt();
        // Сравниваем числа; Рассматриваем 16 случаев; Выводим ответ
        if (X >= A) {
            if (2*X - A >= B) {
                if (3*X - A - B >= C) {
                    if (4*X - A - B - C >= D) {
                        out.print(4);
                    } else {
                        out.print(3);
                    }
                } else {
                    if (X >= D) {
                        out.print(3);
                    } else {
                        out.print(2);
                    }
                }
            } else {
                if (X >= C) {
                    if (2*X - C >= D) {
                        out.print(2);
                    } else {
                        out.print(1);
                    }
                } else {
                    if (X >= D) {
                        out.print(2);
                    } else {
                        out.print(1);
                    }
                }
        }
    } else {
            if (X >= B) {
                if (2*X - B >= C) {
                    if (3*X - B - C >= D) {
                        out.print(3);
                    } else {
                        out.print(2);
                    }
                } else {
                    if (X >= D) {
                        out.print(2);
                    } else {
                        out.print(1);
                    }
                }
            } else {
                if (X >= C) {
                    if (2*X - C >= D) {
                        out.print(2);
                    } else {
                        out.print(1);
                    }
                } else {
                    if (X >= D) {
                        out.print(1);
                    } else {
                        out.print(0);
                    }
                }
            }
        }
    }
}
```

### 6. Анализ правильности решения

1. Тест на `X >= A` и `2X - A >= B` и `3X - A - B >= C` и `4X - A - B - C >= D` :
   - **Input**:
        ```
        10 1 1 1 1
        ```

    - **Output**:
        ```
        4
        ```
2.  Тест на `X >= A` и `2X - A >= B` и `3X - A - B >= C` и `4X - A - B - C < D` :
    - **Input**:
        ```
        10 1 1 1 38
        ```
    - **Output**:
        ```
        3
        ```
3.  Тест на `X >= A` и `2X - A >= B` и `3X - A - B < C` и `X >= D` :
    - **Input**:
        ```
        10 1 1 29 1
        ```

    - **Output**:
        ```
        3
        ```
4. Тест на `X >= A` и `2X - A >= B` и `3X - A - B < C` и `X < D` :
    - **Input**:
        ```
        10 1 1 29 11
        ```

    - **Output**:
        ```
        2
        ```
5. Тест на `X >= A` и `2X - A < B` и `X >= C` и `2X - C >= D` :
   
    - **Input**:
        ```
        10 1 20 1 1
        ```

    - **Output**:
        ```
        3
        ```
6. Тест на `X >= A` и `2X - A < B` и `X >= C` и `2X - C < D` :
    - **Input**:
        ```
        10 1 20 1 20
        ```

    - **Output**:
        ```
        2
        ```
7. Тест на `X >= A` и `2X - A < B` и `X < C` и `X >= D` :
    - **Input**:
        ```
        10 1 20 11 1
        ```

    - **Output**:
        ```
        2
        ```
8. Тест на `X >= A` и `2X - A < B` и `X < C` и `X < D` :
   - **Input**:
        ```
        10 1 20 11 11
        ```

    - **Output**:
        ```
        1
        ```
9. Тест на `X < A` и `X >= B` и `2X - B >= C` и `3X - B - C >= D` :
   - **Input**:
        ```
        10 11 1 1 1
        ```

    - **Output**:
        ```
        3
        ```
10. Тест на `X < A` и `X >= B` и `2X - B >= C` и `3X - B - C < D` :
    - **Input**:
        ```
        10 11 1 1 29
        ```

    - **Output**:
        ```
        2
        ```
11. Тест на `X < A` и `X >= B` и `2X - B < C` и `X >= D` :
    - **Input**:
        ```
        10 11 1 20 1
        ```

    - **Output**:
        ```
        2
        ```
12. Тест на `X < A` и `X >= B` и `2X - B < C` и `X < D` :
    - **Input**:
        ```
        10 11 1 20 11
        ```

    - **Output**:
        ```
        1
        ```
13. Тест на `X < A` и `X < B` и `X >= C` и `2X - C >= D` :
    - **Input**:
        ```
        10 11 11 1 1
        ```

    - **Output**:
        ```
        2
        ```
14. Тест на `X < A` и `X < B` и `X >= C` и `2X - C < D` :
    - **Input**:
        ```
        10 11 11 1 20
        ```

    - **Output**:
        ```
        1
        ```
15. Тест на `X < A` и `X < B` и `X < C` и `X >= D` :
    - **Input**:
        ```
        10 11 11 11 1
        ```

    - **Output**:
        ```
        1
        ```
16. Тест на `X < A` и `X < B` и `X < C` и `X < D` :
    - **Input**:
        ```
        10 11 11 11 11
        ```

    - **Output**:
        ```
        0
        ```
